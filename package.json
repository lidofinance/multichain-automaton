{
  "name": "multichain-automaton",
  "packageManager": "yarn@4.5.1",
  "scripts": {
    "start": "ts-node src/deploy.ts",
    "postinstall": "cd diffyscan && yarn && poetry install && cd ../lido-l2-with-steth && npm install && npm run compile && cd ../state-mate && yarn && cd ../governance-crosschain-bridges && npm install && npm run compile && cd .."
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "postinstall": "^0.7.5",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.8",
    "hardhat": "^2.22.14",
    "hardhat-deploy": "^0.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
